# Example environment variables - DO NOT commit real credentials
# Copy this to .env and fill with real values

# Celery/Redis (if using workers)
REDIS_URL=redis://localhost:6379/0

# MongoDB (if using MongoDB)
MONGODB_URI=mongodb+srv://user:password@cluster/database?retryWrites=true&w=majority

# Backend API URL (for frontend proxy)
BACKEND_URL=https://crm-plus-production.up.railway.app

# Frontend public API URL
NEXT_PUBLIC_API_BASE_URL=https://crm-plus-production.up.railway.app
