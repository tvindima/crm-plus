NEXT_PUBLIC_API_BASE_URL=https://api.example.com
# Backend público (FastAPI) acessível via HTTPS (produçao/UAT/staging)

CRMPLUS_AUTH_SECRET=changeme_super_secret
# Mesmo segredo usado no backend para validar JWT no middleware do backoffice.

NEXT_PUBLIC_SENTRY_DSN=
# Opcional. Deixa vazio para desativar monitorização no front.
